

#### @ObjectModel.semanticKey: []
> 可以通过使用注解 @ObjectModel.semanticKey 来定义语义关键字段，该注解包含实体中的一个或多个属性数组。所指定的字段将以粗体显示，并且还会显示其编辑状态。
>

cds
```
@ObjectModel.semanticKey: ['Name']
```

![alt text](PNG/ObjectModel.semanticKey.png)